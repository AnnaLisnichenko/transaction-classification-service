# Transaction Classification Service
ML service for financial transaction classification.

## Project Structure
- `notebooks/`: Data exploration and model training.
- `data/`: Raw and processed datasets (excluded from Git).
- `src/`: Source code for the API and model inference.